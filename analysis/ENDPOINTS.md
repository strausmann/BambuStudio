# Captured endpoints — findings (sanitized)

> Filled in by the capture session. **Schema/structure + words only — no real values.**
> Detailed body schemas live in `endpoints.schema.json` (machine-readable).

## Status

- [ ] Filament list (GET) captured
- [ ] Filament create (POST) captured
- [ ] Filament update (PUT) captured
- [ ] Filament delete (DELETE) captured
- [ ] (optional) Preset/profile sync (`put_setting`) captured
- Bambu host observed: `__________` (e.g. api.bambulab.com / .cn)
- Auth header style: `__________` (e.g. `Authorization: Bearer <jwt>` — describe, don't paste)

## Endpoint table

| Operation | Method | Host | Path | Query keys | Status |
|-----------|--------|------|------|-----------|--------|
| Filament list | GET | | | | |
| Filament create | POST | | | | |
| Filament update | PUT/PATCH | | | | |
| Filament delete | DELETE | | | | |

## Notes / anomalies

- Pagination: (offset/limit? cursor? total field name?)
- Response wrapper key(s): (e.g. `filaments`, `data`, …)
- Error format on 4xx (structure only):
- Differences vs. the hypothesis `/v1/user-service/my/filament/v2`:
- Anything surprising:

## Open questions back to the design session

-
